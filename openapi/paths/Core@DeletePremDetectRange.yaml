post:
  tags:
    - Additional APIs
  summary: Deletes an IP Range local to the customer network.
  parameters:
    - name: uuid
      in: query
      description: Uuid of IP range to remove
      required: true
      schema:
        type: string
  responses:
    '200':
      description: API-Result
      content:
        '*/*':
          schema:
            $ref: ../components/schemas/CoreDeletePremDetectRange.yaml
  x-idap-anon: false
