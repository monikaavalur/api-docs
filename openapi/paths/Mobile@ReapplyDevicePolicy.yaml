post:
  tags:
    - Additional APIs
  summary: Reapply device policies (Mobile + OSX)
  parameters:
    - name: deviceID
      in: query
      description: Device ID
      required: true
      schema:
        type: string
  responses:
    '200':
      description: API-Result
      content:
        '*/*':
          schema:
            $ref: ../components/schemas/MobileReapplyDevicePolicy.yaml
  x-idap-anon: false
