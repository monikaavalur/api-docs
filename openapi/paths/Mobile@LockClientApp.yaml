post:
  tags:
    - Additional APIs
  summary: Lock client app (Mobile)
  parameters:
    - name: deviceID
      in: query
      description: Device ID
      required: true
      schema:
        type: string
  responses:
    '200':
      description: API-Result
      content:
        '*/*':
          schema:
            $ref: ../components/schemas/MobileLockClientApp.yaml
  x-idap-anon: false
