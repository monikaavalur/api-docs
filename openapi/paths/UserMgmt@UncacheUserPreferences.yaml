post:
  tags:
    - Additional APIs
    - User Management
  summary: Uncache the user preferences.
  parameters:
    - name: userUuidOrName
      in: query
      description: Unique identifier or name of user.
      required: true
      schema:
        type: string
  responses:
    '200':
      description: API-Result
      content:
        '*/*':
          schema:
            $ref: ../components/schemas/UserMgmtUncacheUserPreferences.yaml
  x-idap-anon: false
