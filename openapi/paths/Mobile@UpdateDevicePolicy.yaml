post:
  tags:
    - Additional APIs
  summary: Update device policies (Mobile + OSX)
  parameters:
    - name: deviceID
      in: query
      description: Device ID
      required: true
      schema:
        type: string
  responses:
    '200':
      description: API-Result
      content:
        '*/*':
          schema:
            $ref: ../components/schemas/MobileUpdateDevicePolicy.yaml
  x-idap-anon: false
