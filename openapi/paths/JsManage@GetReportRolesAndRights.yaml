post:
  tags:
    - Additional APIs
    - Role Management
  summary: List the Report roles and rights.
  parameters:
    - name: _RowKey
      in: query
      description: The unique identifier for the reports role.
      required: true
      schema:
        type: string
  responses:
    '200':
      description: API-Result
      content:
        '*/*':
          schema:
            $ref: ../components/schemas/JsManageGetReportRolesAndRights.yaml
  x-idap-anon: false
