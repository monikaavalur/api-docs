post:
  tags:
    - Additional APIs
  summary: Gets the oath otp name for the user.
  responses:
    '200':
      description: API-Result
      content:
        '*/*':
          schema:
            $ref: ../components/schemas/PolicyGetOathOtpClientName.yaml
  x-idap-anon: false
