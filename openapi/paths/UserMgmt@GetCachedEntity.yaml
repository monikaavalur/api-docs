post:
  tags:
    - Additional APIs
    - User Management
  summary: Get the cached entity.
  parameters:
    - name: uuidOrName
      in: query
      description: Unique identifier or name of entity.
      required: true
      schema:
        type: string
  responses:
    '200':
      description: API-Result
      content:
        '*/*':
          schema:
            $ref: ../components/schemas/UserMgmtGetCachedEntity.yaml
  x-idap-anon: false
