post:
  tags:
    - Additional APIs
  summary: Reset Samsung KNOX container password (Samsung Mobile)
  parameters:
    - name: deviceID
      in: query
      description: Device ID
      required: true
      schema:
        type: string
  responses:
    '200':
      description: API-Result
      content:
        '*/*':
          schema:
            $ref: ../components/schemas/MobileKnoxResetContainerPassword.yaml
  x-idap-anon: false
