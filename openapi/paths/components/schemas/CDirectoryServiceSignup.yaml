type: object
properties:
  Result:
    type: object
    properties: {}
    description: >-
      UUID of the created user, authtoken if AutoLogin true,  verificationid and
      status if IsEmailVerificationRequired true.
  Error:
    type: object
    description: Error message text on failure, may be null
