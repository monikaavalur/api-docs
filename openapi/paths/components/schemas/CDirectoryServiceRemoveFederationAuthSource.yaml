type: object
properties:
  Result:
    type: object
    properties: {}
    description: >-
      Standard reponse with 'Completed' is run syncronously, or the Job Id if
      run asyncronously.
  Error:
    type: object
    description: Error message text on failure, may be null
