post:
  tags:
    - Policy Management
  summary: Get policy block.
  parameters:
    - name: name
      in: query
      description: The policy block to get.
      required: true
      schema:
        type: string
  responses:
    '200':
      description: API-Result
      content:
        '*/*':
          schema:
            $ref: ../components/schemas/PolicyGetNicePolicyBlock.yaml
  x-idap-anon: false
