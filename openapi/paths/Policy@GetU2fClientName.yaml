post:
  tags:
    - Additional APIs
  summary: Gets the oath u2f name for the user.
  responses:
    '200':
      description: API-Result
      content:
        '*/*':
          schema:
            $ref: ../components/schemas/PolicyGetU2fClientName.yaml
  x-idap-anon: false
