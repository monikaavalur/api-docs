post:
  tags:
    - Additional APIs
  summary: Gets the public part of the Service Provider signing certificate authority.
  responses:
    '200':
      description: API-Result
      content:
        '*/*':
          schema:
            $ref: ../components/schemas/FederationSPSigningCertificateAuthority.yaml
  x-idap-anon: true
