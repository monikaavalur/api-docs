post:
  tags:
    - Device Management
  summary: Enable SSO on device (Mobile + OSX)
  parameters:
    - name: deviceID
      in: query
      description: Device ID
      required: true
      schema:
        type: string
  responses:
    '200':
      description: API-Result
      content:
        '*/*':
          schema:
            $ref: ../components/schemas/MobileEnableSSO.yaml
  x-idap-anon: false
