post:
  tags:
    - Additional APIs
  summary: Set a device as primary (Mobile)
  parameters:
    - name: deviceID
      in: query
      description: Device ID
      required: true
      schema:
        type: string
  responses:
    '200':
      description: API-Result
      content:
        '*/*':
          schema:
            $ref: ../components/schemas/MobileSetPrimaryDevice.yaml
  x-idap-anon: false
