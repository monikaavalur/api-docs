post:
  tags:
    - Additional APIs
  summary: 'DisablePremDetectRange: Disable OnPrem Detect Range'
  parameters:
    - name: uuid
      in: query
      required: true
      schema:
        type: string
  responses:
    '200':
      description: API-Result
      content:
        '*/*':
          schema:
            $ref: ../components/schemas/CoreDisablePremDetectRange.yaml
  x-idap-anon: false
