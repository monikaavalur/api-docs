post:
  tags:
    - Additional APIs
    - User Management
  summary: Get the cached user.
  parameters:
    - name: uuidOrName
      in: query
      description: Unique identifier or name of user.
      required: true
      schema:
        type: string
  responses:
    '200':
      description: API-Result
      content:
        '*/*':
          schema:
            $ref: ../components/schemas/UserMgmtGetCachedUser.yaml
  x-idap-anon: false
