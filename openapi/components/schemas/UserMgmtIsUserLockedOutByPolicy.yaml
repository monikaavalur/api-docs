type: object
properties:
  Result:
    type: object
    properties: {}
    description: >-
      True if the user is locked out (i.e. cannot answer MFA challenges), false
      otherwise.
  Error:
    type: object
    description: Error message text on failure, may be null
