type: object
properties:
  Result:
    type: object
    properties: {}
    description: >-
      User certificate information.  Dictionary of string key to string value,
      where keys are the certificate thumbprint and values are the certificate
      type.
  Error:
    type: object
    description: Error message text on failure, may be null
